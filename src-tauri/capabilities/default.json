{
  "$schema": "../node_modules/@tauri-apps/cli/schema/capability.json",
  "identifier": "default",
  "description": "Default capability for the Nidham main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
